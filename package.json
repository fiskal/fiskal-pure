{
  "name": "fiskal-antifragile",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/ts",
    "apps/ts-demo"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspace=packages/ts",
    "dev": "npm run dev --workspace=apps/ts-demo"
  }
}
